name: loka
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^4.0.3


dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.12.0"

flutter:
  uses-material-design: true

# KONFIGURASI PUBKU
flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icons/loka_icon.png"